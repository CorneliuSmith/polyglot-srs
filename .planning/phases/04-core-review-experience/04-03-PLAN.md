---
phase: 04-core-review-experience
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - frontend/src/features/auth/LoginPage.tsx
  - frontend/src/features/dashboard/DashboardPage.tsx
  - frontend/src/features/dashboard/DueCount.tsx
  - frontend/src/features/dashboard/StreakBadge.tsx
  - frontend/src/features/dashboard/CEFRProgress.tsx
  - frontend/src/components/LanguagePicker.tsx
  - frontend/src/components/ProtectedRoute.tsx
  - frontend/src/App.tsx
  - frontend/src/__tests__/DashboardPage.test.tsx
autonomous: true
requirements: [UX-01, PROF-02, REV-07, REV-08]

must_haves:
  truths:
    - "User sees due card count for their active language on the dashboard"
    - "User sees their current review streak in days"
    - "User sees CEFR progress bars per level"
    - "User can switch active language via a picker, and dashboard updates"
    - "Dashboard has buttons to start a Learn session or Review session"
  artifacts:
    - path: "frontend/src/features/dashboard/DashboardPage.tsx"
      provides: "Main dashboard with due count, streak, CEFR progress, session start buttons"
    - path: "frontend/src/components/LanguagePicker.tsx"
      provides: "Language selection dropdown updating prefsStore"
    - path: "frontend/src/features/auth/LoginPage.tsx"
      provides: "Supabase auth login page with email/password and Google OAuth"
  key_links:
    - from: "frontend/src/features/dashboard/DashboardPage.tsx"
      to: "/api/dashboard/{language_id}"
      via: "TanStack Query useQuery"
      pattern: "useQuery.*dashboard"
    - from: "frontend/src/components/LanguagePicker.tsx"
      to: "frontend/src/stores/prefsStore.ts"
      via: "setActiveLanguageId"
      pattern: "setActiveLanguageId"
---

<objective>
Build the login page and dashboard — the first screens users see after authenticating.

Purpose: The dashboard is the user's home base, showing their active language stats and providing entry points to Learn and Review sessions. The login page gates access.

Output: Working login flow + dashboard page with live data from GET /api/dashboard/{language_id}.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-review-experience/04-RESEARCH.md
@.planning/phases/04-core-review-experience/04-02-SUMMARY.md

<interfaces>
<!-- From 04-02 (frontend scaffold) -->
From frontend/src/api/types.ts:
```typescript
interface DashboardStats { due_count: number; streak_days: number; cefr_progress: Record<string, number> }
interface Language { id: string; code: string; name: string; rtl: boolean }
interface UserProfile { id: string; batch_size: number; ui_language: string; active_language_id: string | null }
```

From frontend/src/api/dashboard.ts:
```typescript
getDashboardStats(languageId: string): Promise<DashboardStats>
```

From frontend/src/api/profile.ts:
```typescript
getProfile(): Promise<UserProfile>
updateProfile(data: ProfileUpdate): Promise<UserProfile>
getLanguages(): Promise<Language[]>
```

From frontend/src/api/review.ts:
```typescript
startLearnSession(languageId: string): Promise<LearnResponse>
```

From frontend/src/stores/prefsStore.ts:
```typescript
activeLanguageId: string | null
setActiveLanguageId(id: string): void
```

From frontend/src/stores/authStore.ts:
```typescript
session: Session | null
isAuthenticated: boolean
```

From frontend/src/lib/supabase.ts:
```typescript
supabase: SupabaseClient
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login page with Supabase email/password and Google OAuth</name>
  <files>frontend/src/features/auth/LoginPage.tsx, frontend/src/components/ProtectedRoute.tsx, frontend/src/App.tsx</files>
  <action>
1. Create `frontend/src/features/auth/LoginPage.tsx`:
   - Two-tab form: "Sign In" and "Sign Up"
   - Email + password fields with Tailwind styling (clean, centered card layout)
   - "Sign in with Google" button using `supabase.auth.signInWithOAuth({ provider: 'google' })`
   - Email/password sign in: `supabase.auth.signInWithPassword({ email, password })`
   - Email/password sign up: `supabase.auth.signUp({ email, password })`
   - Show error messages from Supabase auth errors
   - On successful auth, the onAuthStateChange listener in App.tsx handles redirect
   - Touch-friendly: 44px+ min-height on all buttons and inputs

2. Create `frontend/src/components/ProtectedRoute.tsx`:
   - Reads isAuthenticated from authStore
   - If not authenticated, Navigate to /login
   - If authenticated, render children (Outlet)
   - Show a loading spinner while session is being restored (session === null and we haven't checked yet — add a `loading` boolean to authStore that starts true and becomes false after first getSession() call)

3. Update `frontend/src/App.tsx`:
   - Replace placeholder routes with real components
   - Wrap dashboard/review/learn routes in ProtectedRoute
   - Add `loading` state to authStore initialization
   - Update authStore to include `loading: boolean`, `setLoading(b)` — set false after initial getSession()
  </action>
  <verify>
    <automated>cd /Users/isaiahcsmith/Documents/gsd-tests/polyglot-srs/frontend && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Login page renders with email/password form and Google OAuth button. Protected routes redirect to login when not authenticated.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page with language picker, due count, streak, CEFR progress, and session buttons</name>
  <files>frontend/src/features/dashboard/DashboardPage.tsx, frontend/src/features/dashboard/DueCount.tsx, frontend/src/features/dashboard/StreakBadge.tsx, frontend/src/features/dashboard/CEFRProgress.tsx, frontend/src/components/LanguagePicker.tsx, frontend/src/__tests__/DashboardPage.test.tsx</files>
  <action>
1. Create `frontend/src/components/LanguagePicker.tsx`:
   - Uses TanStack Query to fetch languages via `getLanguages()`
   - Renders a `<select>` dropdown with language names
   - On change, calls `prefsStore.setActiveLanguageId(selectedId)` AND `updateProfile({ active_language_id: selectedId })` to persist server-side
   - Shows current active language as selected
   - If no activeLanguageId in store, auto-select the first language from the API response and update both store + server
   - Min-height 44px, touch-friendly

2. Create `frontend/src/features/dashboard/DueCount.tsx`:
   - Displays due card count as a large number with label "Cards Due"
   - Takes `count: number` as prop
   - Styled as a card with shadow, large text

3. Create `frontend/src/features/dashboard/StreakBadge.tsx`:
   - Displays streak as "{N} day streak" with a flame icon (use unicode or simple SVG)
   - Takes `days: number` as prop
   - Shows "Start your streak!" when days === 0

4. Create `frontend/src/features/dashboard/CEFRProgress.tsx`:
   - Renders A1 through C2 progress bars
   - Takes `progress: Record<string, number>` as prop (values 0.0 to 1.0)
   - Each level: label, progress bar (Tailwind bg-primary), percentage text
   - Levels with 0 progress still shown (empty bar) so user sees what's ahead
   - Use Tailwind logical properties (ms-*, me-*) for RTL compatibility

5. Create `frontend/src/features/dashboard/DashboardPage.tsx`:
   - Reads activeLanguageId from prefsStore
   - Uses TanStack Query: `useQuery({ queryKey: ['dashboard', activeLanguageId], queryFn: () => getDashboardStats(activeLanguageId!), enabled: !!activeLanguageId })`
   - Renders LanguagePicker at top
   - Grid layout: DueCount and StreakBadge side by side, CEFRProgress below
   - Two action buttons at bottom:
     - "Learn New Cards" — navigates to /learn (or calls startLearnSession then navigates to /review)
     - "Review Due Cards" — navigates to /review
   - Both buttons disabled when due_count === 0 (for Review) or when loading
   - Loading state: skeleton placeholders while query loads
   - Mobile: stack vertically on small screens, grid on lg+

6. Create `frontend/src/__tests__/DashboardPage.test.tsx`:
   - Test that DueCount renders the count
   - Test that StreakBadge renders days
   - Test that CEFRProgress renders all 6 levels
   - Mock TanStack Query or test components individually with props
  </action>
  <verify>
    <automated>cd /Users/isaiahcsmith/Documents/gsd-tests/polyglot-srs/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Dashboard page renders with language picker, due count, streak badge, CEFR progress bars, and Learn/Review buttons. All dashboard component tests pass.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vite build` exits 0
- `cd frontend && npx vitest run` exits 0
- Dashboard components render with correct data shapes
- LanguagePicker updates both local store and server profile
</verification>

<success_criteria>
- Login page supports email/password and Google OAuth via Supabase
- Protected routes redirect unauthenticated users to /login
- Dashboard shows due count, streak days, CEFR progress for active language
- Language picker switches active language and updates dashboard data
- Learn and Review buttons navigate to respective pages
- Mobile responsive layout (stacked on small screens)
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-review-experience/04-03-SUMMARY.md`
</output>
