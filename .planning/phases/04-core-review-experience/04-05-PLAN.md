---
phase: 04-core-review-experience
plan: 05
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - frontend/src/components/LanguageWrapper.tsx
  - frontend/src/features/keyboards/OnScreenKeyboard.tsx
  - frontend/src/features/review/DrillCard.tsx
  - frontend/src/features/review/ReviewSessionPage.tsx
  - frontend/src/__tests__/LanguageWrapper.test.tsx
  - frontend/src/__tests__/OnScreenKeyboard.test.tsx
autonomous: false
requirements: [UX-02, UX-03, UX-08]

must_haves:
  truths:
    - "Arabic content renders in RTL direction with Noto Naskh Arabic font"
    - "Russian review sessions show a Cyrillic on-screen keyboard"
    - "Arabic review sessions show an Arabic on-screen keyboard"
    - "English review sessions show no on-screen keyboard"
    - "On-screen keyboard key presses insert characters into the drill input"
    - "All interactive elements have 44px+ touch targets"
    - "Layout is mobile-responsive at 320px, 375px, and 768px widths"
  artifacts:
    - path: "frontend/src/components/LanguageWrapper.tsx"
      provides: "RTL wrapper with dir attribute and Arabic font class"
    - path: "frontend/src/features/keyboards/OnScreenKeyboard.tsx"
      provides: "Virtual keyboard for Russian and Arabic input"
  key_links:
    - from: "frontend/src/features/review/ReviewSessionPage.tsx"
      to: "frontend/src/features/keyboards/OnScreenKeyboard.tsx"
      via: "conditional render based on language_code"
      pattern: "OnScreenKeyboard"
    - from: "frontend/src/features/review/DrillCard.tsx"
      to: "frontend/src/components/LanguageWrapper.tsx"
      via: "wraps card content for RTL"
      pattern: "LanguageWrapper"
---

<objective>
Add RTL layout support for Arabic, on-screen keyboards for Cyrillic and Arabic, and ensure mobile responsiveness across the app.

Purpose: Arabic learners need RTL text rendering and an Arabic on-screen keyboard. Russian learners need a Cyrillic keyboard. All users need a mobile-friendly interface with proper touch targets.

Output: RTL-aware drill cards, working virtual keyboards, and mobile-responsive layout verified visually.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-review-experience/04-RESEARCH.md
@.planning/phases/04-core-review-experience/04-02-SUMMARY.md
@.planning/phases/04-core-review-experience/04-04-SUMMARY.md

<interfaces>
<!-- From 04-RESEARCH.md patterns -->

react-simple-keyboard usage:
```tsx
import Keyboard from 'react-simple-keyboard'
import 'react-simple-keyboard/build/css/index.css'
import russianLayout from 'simple-keyboard-layouts/build/layouts/Russian'
import arabicLayout from 'simple-keyboard-layouts/build/layouts/Arabic'
```

Tailwind v4 logical properties (use instead of ml/mr/pl/pr):
- ms-* (margin-inline-start), me-* (margin-inline-end)
- ps-* (padding-inline-start), pe-* (padding-inline-end)
- border-s-* (border-inline-start), border-e-* (border-inline-end)
- text-start, text-end (instead of text-left, text-right)

From frontend/src/api/types.ts:
```typescript
interface DueCard { language_code: string; ... }
interface Language { code: string; rtl: boolean; ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LanguageWrapper component for RTL and OnScreenKeyboard for Cyrillic/Arabic with tests</name>
  <files>frontend/src/components/LanguageWrapper.tsx, frontend/src/features/keyboards/OnScreenKeyboard.tsx, frontend/src/__tests__/LanguageWrapper.test.tsx, frontend/src/__tests__/OnScreenKeyboard.test.tsx</files>
  <action>
1. Create `frontend/src/components/LanguageWrapper.tsx`:
   - Props: `children: React.ReactNode`, `languageCode: string`
   - Sets `dir="rtl"` when languageCode === 'ar', `dir="ltr"` otherwise
   - Applies `font-['Noto_Naskh_Arabic'] text-xl leading-loose` classes when Arabic
   - Does NOT apply dir to the entire page -- only wraps the content area (drill card sentence, vocabulary display)
   - Uses clsx for conditional classes

2. Create `frontend/src/features/keyboards/OnScreenKeyboard.tsx`:
   - Props: `languageCode: 'ru' | 'ar'`, `onKeyPress: (key: string) => void`, `inputRef?: React.RefObject<HTMLInputElement>`
   - Import Keyboard from react-simple-keyboard and CSS
   - Import Russian and Arabic layouts from simple-keyboard-layouts
   - Select layout based on languageCode
   - On key press: call onKeyPress with the character
   - Support shift key for uppercase/alternate characters (keyboard component handles this natively)
   - Style: full width below the drill card, max-w-lg centered, border-t for visual separation
   - Do NOT render for English (parent component handles this conditional)

3. Create `frontend/src/__tests__/LanguageWrapper.test.tsx`:
   - Test: Arabic language code sets dir="rtl" on wrapper
   - Test: Russian language code sets dir="ltr" on wrapper
   - Test: Arabic applies Noto Naskh Arabic font class

4. Create `frontend/src/__tests__/OnScreenKeyboard.test.tsx`:
   - Test: Keyboard renders for 'ru' language code
   - Test: Keyboard renders for 'ar' language code
   - Test: onKeyPress callback is called (may need to mock react-simple-keyboard)
  </action>
  <verify>
    <automated>cd /Users/isaiahcsmith/Documents/gsd-tests/polyglot-srs/frontend && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>LanguageWrapper correctly sets dir and font for Arabic. OnScreenKeyboard renders Russian and Arabic layouts. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate RTL wrapper and keyboard into review session, audit mobile responsiveness</name>
  <files>frontend/src/features/review/DrillCard.tsx, frontend/src/features/review/ReviewSessionPage.tsx</files>
  <action>
1. Update `frontend/src/features/review/DrillCard.tsx`:
   - Wrap the sentence rendering in `<LanguageWrapper languageCode={languageCode}>` (add languageCode to props)
   - Ensure the `<input>` element gets `dir` matching the language (so cursor and text direction are correct for Arabic)
   - Audit all Tailwind classes: replace any physical properties (ml-*, mr-*, pl-*, pr-*, text-left, text-right) with logical equivalents (ms-*, me-*, ps-*, pe-*, text-start, text-end)

2. Update `frontend/src/features/review/ReviewSessionPage.tsx`:
   - Add OnScreenKeyboard below DrillCard when `currentCard.language_code === 'ru' || currentCard.language_code === 'ar'`
   - Wire keyboard onKeyPress to insert characters at cursor position in the drill input:
     - Track input ref via useRef<HTMLInputElement>
     - On keyboard key press: get current value, insert character at selectionStart, update value, set cursor position
   - Do NOT show keyboard for English (language_code === 'en')
   - Keyboard should be dismissible (optional toggle button: "Show/Hide Keyboard")

3. Mobile responsiveness audit across all Phase 4 components:
   - LoginPage: max-w-sm centered, inputs full-width, 44px+ buttons
   - DashboardPage: grid-cols-1 on mobile, grid-cols-2 on md+, 44px+ action buttons
   - ReviewSessionPage: card centered, max-w-2xl, keyboard full-width below
   - RatingButtons: grid-cols-2 on mobile, grid-cols-4 on md+, min-h-[44px] on all
   - SessionSummary: centered card, full-width on mobile
   - All form inputs: min-h-[44px], text-base (16px min to prevent iOS zoom)
   - Add `touch-manipulation` to all interactive elements (prevents 300ms delay)
  </action>
  <verify>
    <automated>cd /Users/isaiahcsmith/Documents/gsd-tests/polyglot-srs/frontend && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Arabic drill cards render RTL with correct font. On-screen keyboards appear for Russian and Arabic. All interactive elements have 44px+ touch targets. Layout is mobile-responsive.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete review experience with RTL layout, on-screen keyboards, and mobile responsiveness</what-built>
  <how-to-verify>
    1. Start backend: `cd /path/to/polyglot-srs && uvicorn backend.main:app --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Open http://localhost:5173 in browser

    **Login flow:**
    - Verify login page renders with email/password fields and Google OAuth button
    - Sign up with test credentials (or use existing Supabase account)
    - Verify redirect to dashboard after login

    **Dashboard (per language):**
    - Select Russian from language picker -- verify due count, streak, CEFR progress load
    - Select Arabic -- verify data updates for Arabic
    - Verify "Learn New Cards" and "Review Due Cards" buttons visible

    **Review session:**
    - Click "Review Due Cards" (if cards are due)
    - Verify fill-in-the-blank card renders with input field at {{answer}} position
    - Type an answer and press Enter
    - Verify NLP feedback panel appears (may show "NLP unavailable" if backends not loaded -- that's OK for visual check)
    - Verify rating buttons (Again/Hard/Good/Easy) are visible and clickable
    - Complete session -- verify summary with accuracy %, time, card count

    **RTL (Arabic):**
    - Switch to Arabic language, start a review
    - Verify text direction is right-to-left
    - Verify Arabic font (Noto Naskh Arabic) is applied to card content
    - Verify on-screen Arabic keyboard appears below card

    **Keyboard (Russian):**
    - Switch to Russian, start a review
    - Verify on-screen Cyrillic keyboard appears
    - Press keys -- verify characters appear in input field
    - Switch to English -- verify NO on-screen keyboard

    **Mobile:**
    - Resize browser to 375px width (or use DevTools mobile simulation)
    - Verify dashboard stacks vertically
    - Verify rating buttons are 2 columns on mobile
    - Verify all buttons/inputs are at least 44px tall
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx vite build` exits 0
- `cd frontend && npx vitest run --reporter=verbose` exits 0
- Visual verification of RTL, keyboards, and mobile responsiveness
</verification>

<success_criteria>
- Arabic content renders RTL with Noto Naskh Arabic font
- On-screen keyboards work for Russian (Cyrillic) and Arabic
- No keyboard shown for English
- Keyboard key presses insert characters into drill input
- All interactive elements have 44px+ min-height
- Layout is mobile-responsive at 320px, 375px, 768px breakpoints
- Human visual verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-review-experience/04-05-SUMMARY.md`
</output>
