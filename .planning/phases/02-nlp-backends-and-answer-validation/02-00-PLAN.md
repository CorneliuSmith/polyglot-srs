---
phase: 02-nlp-backends-and-answer-validation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_nlp_base.py
  - backend/tests/test_nlp_russian.py
  - backend/tests/test_nlp_arabic.py
  - backend/tests/test_nlp_english.py
autonomous: true
requirements: [NLP-01, NLP-02, NLP-03, NLP-04, NLP-05, NLP-06, NLP-07, NLP-08, NLP-09, NLP-10]

must_haves:
  truths:
    - "All four test files exist and contain failing tests before any implementation begins"
    - "Tests define expected behavior contracts for BaseNLP and all three language backends"
    - "Running pytest on any test file produces RED (failures/errors), not GREEN"
  artifacts:
    - path: "backend/tests/test_nlp_base.py"
      provides: "Failing tests for BaseNLP pipeline, registry, and AnswerResult relocation"
      min_lines: 40
    - path: "backend/tests/test_nlp_russian.py"
      provides: "Failing tests for RussianNLP normalization, lemmatization, transliteration, aspect"
      min_lines: 40
    - path: "backend/tests/test_nlp_arabic.py"
      provides: "Failing tests for ArabicNLP normalization, diacritic invariance, verb form detection"
      min_lines: 40
    - path: "backend/tests/test_nlp_english.py"
      provides: "Failing tests for EnglishNLP normalization, lemmatization, morphological family"
      min_lines: 40
  key_links: []
---

<objective>
Create all four NLP test files with failing tests (RED phase) before any implementation begins.

Purpose: Establishes behavioral contracts via tests first. Each subsequent plan (02-01 through 02-04) turns these tests GREEN. This enforces RED->GREEN TDD separation and satisfies Nyquist sampling requirements.
Output: Four test files, all containing tests that fail (ImportError or AssertionError) because the implementation does not exist yet.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-nlp-backends-and-answer-validation/02-RESEARCH.md
@answer-validation-spec.md
@backend/services/srs.py

<interfaces>
<!-- Existing AnswerResult in srs.py — will be relocated in 02-01 -->
From backend/services/srs.py:
```python
class AnswerResult(Enum):
    CORRECT = "correct"
    CORRECT_SLOPPY = "correct_sloppy"
    WRONG_FORM = "wrong_form"
    WRONG = "wrong"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing test files for BaseNLP and NLP registry (RED)</name>
  <files>backend/tests/test_nlp_base.py</files>
  <action>
    Create `backend/tests/test_nlp_base.py` with tests that will FAIL because `backend/services/nlp/base.py` and `backend/services/nlp/__init__.py` do not exist yet. Tests should import from the not-yet-created modules and assert expected behavior.

    Test cases to include (all should fail with ImportError or similar):

    BaseNLP ABC tests:
    - BaseNLP cannot be instantiated directly (is abstract)
    - Concrete stub implementing all abstract methods can be instantiated
    - Exact match returns (CORRECT, None)
    - Normalized match (case difference) returns (CORRECT, None)
    - Lemma match (same lemma, different inflection) returns (CORRECT_SLOPPY, message)
    - Morphological family match returns (CORRECT_SLOPPY, message)
    - Aspect partner match returns (WRONG_FORM, message) — use signature `get_aspect_partner(verb, card_context=None)`
    - No match returns (WRONG, None)
    - Answer alternatives from card_context checked before WRONG — matching alternative returns (CORRECT, None)
    - NFC normalization handles different Unicode forms correctly

    Registry tests:
    - get_nlp raises ValueError for unknown language code
    - After registering a stub backend, get_nlp returns it
    - init_nlp_backends doesn't crash when backend modules don't exist yet

    AnswerResult relocation tests:
    - AnswerResult imported from nlp.base works with QUALITY_MAP values

    IMPORTANT: The StubNLP used in tests must define `get_aspect_partner(self, verb, card_context=None)` with TWO parameters (plus self) — this is the standardized signature across all plans. Layer 5 in check_answer calls `self.get_aspect_partner(correct, card_context)`.

    Use `pytest.importorskip` or direct imports that will fail. The goal is RED — every test should fail when run.
  </action>
  <verify>
    <automated>cd /Users/isaiahcsmith/Documents/gsd-tests/polyglot-srs && python -m pytest backend/tests/test_nlp_base.py -x -q 2>&1 | head -5 | grep -E "ERROR|FAILED|error"</automated>
  </verify>
  <done>Test file exists with 10+ test functions. Running pytest produces failures (RED). No test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write failing test files for Russian, Arabic, and English backends (RED)</name>
  <files>backend/tests/test_nlp_russian.py, backend/tests/test_nlp_arabic.py, backend/tests/test_nlp_english.py</files>
  <action>
    Create three test files that will FAIL because the backend implementations do not exist yet.

    **backend/tests/test_nlp_russian.py** — tests for RussianNLP (NLP-03, NLP-04, NLP-05):
    - normalize("Собака") -> "собака"
    - lemmatize("собаку") -> "собака"
    - lemmatize("пишет") -> "писать"
    - get_morphological_family("собака") contains at least {"собака", "собаки", "собаке", "собаку"}
    - check_answer("sobaka", "собака") -> (CORRECT_SLOPPY, message about Cyrillic)
    - check_answer("da", "да") -> (CORRECT_SLOPPY, message)
    - get_aspect_partner("писать", {"morphology": {"aspect_partner": "написать"}}) -> "написать"
    - check_answer("писать", "написать", {"morphology": {"aspect_partner": "писать"}}) -> (WRONG_FORM, message)
    - Full pipeline: exact, normalized, lemma, wrong cases

    **backend/tests/test_nlp_arabic.py** — tests for ArabicNLP (NLP-06, NLP-07, NLP-08):
    - normalize strips tashkeel: normalize("كَتَبَ") -> "كتب"
    - normalize handles alef variants
    - normalize strips tatweel
    - Diacritic invariance: check_answer("كتب", "كَتَبَ") -> (CORRECT, None)
    - Taa marbuta vs ha: returns CORRECT_SLOPPY (not CORRECT or WRONG)
    - Verb form detection: same root, different form -> WRONG_FORM with root display
    - lemmatize returns lex field from analysis
    - get_aspect_partner returns None (Arabic has no aspect partner system)
    - Use `pytest.importorskip("camel_tools")` for tests requiring camel-tools data

    **backend/tests/test_nlp_english.py** — tests for EnglishNLP (NLP-09):
    - normalize("The Dog") -> "dog"
    - normalize("A cat") -> "cat"
    - normalize("the") -> "the" (edge case: don't strip if entire word is article)
    - lemmatize("went") -> "go"
    - lemmatize("mice") -> "mouse"
    - get_morphological_family("go") contains at least {"go", "goes", "going", "went", "gone"}
    - get_aspect_partner("go", None) -> None
    - Full pipeline: exact, normalized, lemma, article stripping, alternatives
    - Use `pytest.importorskip("spacy")` for tests requiring spaCy model

    IMPORTANT for all three files:
    - Import from `backend.services.nlp.russian`, `.arabic`, `.english` respectively — these don't exist yet, so imports will fail (RED).
    - Use the standardized `get_aspect_partner(verb, card_context=None)` signature in all test assertions.
  </action>
  <verify>
    <automated>cd /Users/isaiahcsmith/Documents/gsd-tests/polyglot-srs && python -m pytest backend/tests/test_nlp_russian.py backend/tests/test_nlp_arabic.py backend/tests/test_nlp_english.py -x -q 2>&1 | head -5 | grep -E "ERROR|FAILED|error"</automated>
  </verify>
  <done>Three test files exist. Running pytest on each produces failures (RED). No test passes. All test files use standardized get_aspect_partner(verb, card_context=None) signature.</done>
</task>

</tasks>

<verification>
- All four test files exist in backend/tests/
- `python -m pytest backend/tests/test_nlp_base.py -x -q` exits non-zero (RED)
- `python -m pytest backend/tests/test_nlp_russian.py -x -q` exits non-zero (RED)
- `python -m pytest backend/tests/test_nlp_arabic.py -x -q` exits non-zero (RED)
- `python -m pytest backend/tests/test_nlp_english.py -x -q` exits non-zero (RED)
</verification>

<success_criteria>
- Four test files created with comprehensive behavior contracts
- All tests fail (RED) because implementations don't exist
- Standardized get_aspect_partner(verb, card_context=None) signature used consistently
- Tests cover all requirements: NLP-01 through NLP-10
</success_criteria>

<output>
After completion, create `.planning/phases/02-nlp-backends-and-answer-validation/02-00-SUMMARY.md`
</output>
