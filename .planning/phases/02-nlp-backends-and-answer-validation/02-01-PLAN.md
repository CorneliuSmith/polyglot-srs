---
phase: 02-nlp-backends-and-answer-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/nlp/__init__.py
  - backend/services/nlp/base.py
  - backend/services/srs.py
  - backend/main.py
  - backend/tests/test_nlp_base.py
  - pyproject.toml
autonomous: true
requirements: [NLP-01, NLP-02, NLP-10]

must_haves:
  truths:
    - "BaseNLP abstract class exists with normalize, lemmatize, get_morphological_family, get_aspect_partner, check_answer methods"
    - "4-tier check_answer pipeline returns correct AnswerResult for each tier (exact, normalized, lemma, morphological family)"
    - "Answer alternatives from card_context are checked before returning WRONG"
    - "AnswerResult enum lives in nlp/base.py and srs.py imports it from there"
    - "NLP registry can register and retrieve backends by language code"
  artifacts:
    - path: "backend/services/nlp/base.py"
      provides: "BaseNLP ABC with 4-tier check_answer pipeline and AnswerResult enum"
      exports: ["BaseNLP", "AnswerResult"]
    - path: "backend/services/nlp/__init__.py"
      provides: "NLP backend registry with init and get functions"
      exports: ["get_nlp", "init_nlp_backends", "NLP_BACKENDS"]
    - path: "backend/tests/test_nlp_base.py"
      provides: "Unit tests for base pipeline, registry, and alternatives"
  key_links:
    - from: "backend/services/srs.py"
      to: "backend/services/nlp/base.py"
      via: "import AnswerResult"
      pattern: "from backend\\.services\\.nlp\\.base import AnswerResult"
    - from: "backend/main.py"
      to: "backend/services/nlp"
      via: "init_nlp_backends() in lifespan"
      pattern: "init_nlp_backends"
---

<objective>
Create the BaseNLP abstract interface, 4-tier answer validation pipeline, NLP backend registry, and relocate AnswerResult enum. Install NLP dependencies.

Purpose: Establishes the foundation all three language backends build on. The base check_answer pipeline (exact -> normalized -> lemma -> morphological family -> aspect partner -> alternatives) is language-agnostic and testable with a concrete stub.
Output: BaseNLP ABC, registry, relocated AnswerResult, passing base pipeline tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-nlp-backends-and-answer-validation/02-RESEARCH.md
@backend/services/srs.py
@backend/main.py
@answer-validation-spec.md

<interfaces>
<!-- Existing AnswerResult in srs.py that must be relocated -->

From backend/services/srs.py:
```python
class AnswerResult(Enum):
    CORRECT = "correct"
    CORRECT_SLOPPY = "correct_sloppy"
    WRONG_FORM = "wrong_form"
    WRONG = "wrong"

QUALITY_MAP: dict[AnswerResult, int] = {
    AnswerResult.CORRECT: 4,
    AnswerResult.CORRECT_SLOPPY: 3,
    AnswerResult.WRONG_FORM: 2,
    AnswerResult.WRONG: 1,
}

def map_answer_to_quality(result: AnswerResult) -> int:
```

From backend/main.py:
```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    settings = get_settings()
    await init_pool(settings.database_url)
    yield
    await close_pool()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install NLP dependencies, create BaseNLP ABC with 4-tier pipeline, relocate AnswerResult</name>
  <files>pyproject.toml, backend/services/nlp/__init__.py, backend/services/nlp/base.py, backend/services/srs.py, backend/tests/test_nlp_base.py</files>
  <behavior>
    - Test: BaseNLP cannot be instantiated directly (is abstract)
    - Test: Concrete stub implementing all abstract methods can be instantiated
    - Test: Exact match returns (CORRECT, None)
    - Test: Normalized match (case difference) returns (CORRECT, None)
    - Test: Lemma match (same lemma, different inflection) returns (CORRECT_SLOPPY, message)
    - Test: Morphological family match returns (CORRECT_SLOPPY, message)
    - Test: Aspect partner match returns (WRONG_FORM, message)
    - Test: No match returns (WRONG, None)
    - Test: Answer alternatives from card_context checked before WRONG — matching alternative returns (CORRECT, None)
    - Test: NFC normalization handles Cyrillic/Latin lookalikes correctly
    - Test: AnswerResult imported from nlp.base works identically in srs.py (QUALITY_MAP still works)
    - Test: Existing srs.py tests still pass after AnswerResult relocation
  </behavior>
  <action>
    1. Add NLP dependencies to pyproject.toml: pymorphy3, camel-tools, spacy, lemminflect, cyrtranslit. Note in a comment that camel-tools requires Python <=3.12 and system deps (cmake, boost).

    2. Create `backend/services/nlp/__init__.py` — empty for now (registry comes in Task 2).

    3. Create `backend/services/nlp/base.py`:
       - Move `AnswerResult` enum here from srs.py. Keep same values (CORRECT="correct", CORRECT_SLOPPY="correct_sloppy", WRONG_FORM="wrong_form", WRONG="wrong").
       - Define `BaseNLP(ABC)` with abstract methods: `normalize(text: str) -> str`, `lemmatize(word: str) -> str`, `get_morphological_family(word: str) -> set[str]`, `get_aspect_partner(verb: str, card_context: dict | None = None) -> str | None`.
       - Implement `check_answer(user_input: str, correct_answer: str, card_context: dict | None = None) -> tuple[AnswerResult, str | None]` as a concrete template method with 6 layers:
         Layer 1: Exact match after NFC normalization + strip -> CORRECT
         Layer 2: self.normalize(user) == self.normalize(correct) -> CORRECT
         Layer 3: self.lemmatize(user) == self.lemmatize(correct) -> CORRECT_SLOPPY with message
         Layer 4: self.normalize(user) in {self.normalize(f) for f in self.get_morphological_family(correct)} -> CORRECT_SLOPPY with message
         Layer 5: aspect partner check via self.get_aspect_partner(correct, card_context) -> WRONG_FORM with message
         Layer 6: card_context["answer_alternatives"] normalized check -> CORRECT
         Default: WRONG, None
       - Apply `unicodedata.normalize('NFC', ...)` as the FIRST step before any comparison (per research pitfall #1).

    4. Update `backend/services/srs.py`:
       - Remove the `AnswerResult` class definition.
       - Add `from backend.services.nlp.base import AnswerResult` at the top.
       - Keep QUALITY_MAP, map_answer_to_quality unchanged — they now import AnswerResult from nlp.base.

    5. Create `backend/tests/test_nlp_base.py` with a `StubNLP(BaseNLP)` concrete implementation for testing the base pipeline. The stub should have simple, predictable behavior (e.g., normalize = lowercase, lemmatize = strip trailing 's', morphological_family = {word, word+'s'}, get_aspect_partner = lookup from a small dict).

    IMPORTANT: Do NOT normalize taa marbuta in the base normalize — that is language-specific (per research pitfall #6). The base class only applies NFC normalization; each language backend adds its own normalization in its override.
  </action>
  <verify>
    <automated>cd /Users/isaiahcsmith/Documents/gsd-tests/polyglot-srs && python -m pytest backend/tests/test_nlp_base.py backend/tests/test_srs.py -x -q</automated>
  </verify>
  <done>BaseNLP ABC defined with all abstract methods and 6-layer check_answer. AnswerResult relocated to nlp/base.py. srs.py imports from nlp.base. All base pipeline tests and existing SRS tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create NLP backend registry and wire into FastAPI lifespan</name>
  <files>backend/services/nlp/__init__.py, backend/main.py, backend/tests/test_nlp_base.py</files>
  <action>
    1. Implement `backend/services/nlp/__init__.py`:
       - `NLP_BACKENDS: dict[str, BaseNLP] = {}` module-level registry.
       - `init_nlp_backends()` function that lazily imports and instantiates RussianNLP, ArabicNLP, EnglishNLP. Wrap each in try/except so missing backends (not yet implemented) log a warning but don't crash the app. This allows incremental backend development.
       - `get_nlp(language_code: str) -> BaseNLP` that retrieves from registry, raises ValueError if not found.
       - `validate_answer_async(language_code: str, user_input: str, correct_answer: str, card_context: dict | None = None) -> tuple[AnswerResult, str | None]` convenience function using `asyncio.to_thread()`.

    2. Update `backend/main.py` lifespan to call `init_nlp_backends()` after `init_pool()`. Import from `backend.services.nlp`. Wrap in try/except so the app starts even if NLP libraries aren't installed yet (dev convenience).

    3. Add registry tests to `backend/tests/test_nlp_base.py`:
       - Test: get_nlp raises ValueError for unknown language code
       - Test: After registering a stub backend, get_nlp returns it
       - Test: init_nlp_backends doesn't crash when backend modules don't exist yet

    Do NOT install NLP library data (spaCy model, camel_data) in this task — that happens when each backend is implemented in Plans 02-04. The registry gracefully handles missing backends.
  </action>
  <verify>
    <automated>cd /Users/isaiahcsmith/Documents/gsd-tests/polyglot-srs && python -m pytest backend/tests/test_nlp_base.py -x -q</automated>
  </verify>
  <done>NLP registry works: get_nlp retrieves registered backends, raises on unknown codes. init_nlp_backends gracefully handles missing backend modules. validate_answer_async wraps check_answer with asyncio.to_thread. FastAPI lifespan calls init_nlp_backends.</done>
</task>

</tasks>

<verification>
- `python -m pytest backend/tests/test_nlp_base.py backend/tests/test_srs.py -x -q` — all pass
- `python -c "from backend.services.nlp.base import BaseNLP, AnswerResult; print('OK')"` — imports work
- `python -c "from backend.services.srs import AnswerResult, QUALITY_MAP; print(QUALITY_MAP)"` — srs.py still works with relocated enum
</verification>

<success_criteria>
- BaseNLP ABC with 5 abstract methods and 6-layer check_answer pipeline
- AnswerResult enum in nlp/base.py, imported by srs.py
- NLP registry with get_nlp, init_nlp_backends, validate_answer_async
- All existing tests (SRS, auth) still pass
- Base pipeline tests cover all 6 layers + edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/02-nlp-backends-and-answer-validation/02-01-SUMMARY.md`
</output>
